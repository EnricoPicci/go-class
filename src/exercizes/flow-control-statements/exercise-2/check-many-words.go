package main

func checkManyWord(words []string) {
	// check each of the words passed in as a collection (slice) of strings.
	// if a word's length is less than 7 a message has to be printed to warn that the word is too short
	// if a word's length is greater than 16 a message has to be printed to warn that the word is too long
	// otherwise a success message has to be printed
}
