# Worker pool pattern

In this example we implement worker pool pattern in its simplest form

## build

From the GO-CLASS project folder run the command
`go build -o ./bin/worker-pool ./src/concurrency-patterns/worker-pool`

## run

From the GO-CLASS project folder run the command
`./bin/worker-pool`
